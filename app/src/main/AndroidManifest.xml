<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teachinghelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Settings"></activity>
        <activity
            android:name=".temp"
            android:label="@string/title_activity_temp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.AttemptDetailsActivity" />
        <activity android:name=".View.AttemptSummary" />
        <activity android:name=".View.QuestionsActivity" />
        <activity android:name=".View.AreaChoice" />
        <activity android:name=".View.SubjectChoice" />
        <activity android:name=".View.MenuTrigger" />
        <activity
            android:name=".View.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>